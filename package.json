{
  "name": "@workspacejson/datahub-agent",
  "version": "0.0.1",
  "private": true,
  "description": "Public reference DataHub application consuming released workspace.json interfaces: DataHub MCP consumption, dbt source-file joins keyed on repository-relative paths, evaluation and demo assets.",
  "license": "Apache-2.0",
  "author": "workspace-json contributors",
  "homepage": "https://github.com/workspacejson/datahub-agent",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/workspacejson/datahub-agent.git"
  },
  "bugs": {
    "url": "https://github.com/workspacejson/datahub-agent/issues"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "test": "vitest run && npm run test:cockpit",
    "typecheck": "tsc --noEmit && npm run typecheck --workspace=@workspacejson/cockpit",
    "dev": "npm run dev --workspace=@workspacejson/cockpit",
    "build": "npm run build --workspace=@workspacejson/cockpit",
    "test:cockpit": "npm run test --workspace=@workspacejson/cockpit",
    "e2e": "playwright test --config apps/cockpit/playwright.config.ts",
    "check:clean-room": "node scripts/check-clean-room.mjs",
    "validate:asset-registry": "node scripts/validate-asset-registry.mjs",
    "parity:datahub-adapter": "tsx migration/parity-datahub-shim.mjs",
    "lint": "biome lint --error-on-warnings",
    "demo:validate": "node scripts/demo/validate-packet.mjs",
    "demo:audio": "node scripts/demo/generate-narration.mjs",
    "demo:captions": "node scripts/demo/generate-captions.mjs",
    "demo:concat": "node scripts/demo/concatenate-narration.mjs",
    "demo:rehearse": "node scripts/demo/run-storyboard.mjs",
    "demo:capture": "node scripts/demo/obs-director.mjs",
    "demo:verify": "node scripts/demo/verify-output.mjs",
    "verify:judging": "npm run typecheck && npm run check:clean-room && npm test && npm run parity:datahub-adapter"
  },
  "dependencies": {
    "@workspacejson/spec": "0.4.4",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "@biomejs/biome": "2.5.6",
    "@playwright/test": "^1.55.0",
    "@types/node": "22.19.17",
    "@workspacejson/cli": "0.5.0",
    "axe-core": "^4.10.3",
    "tsx": "4.19.2",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
