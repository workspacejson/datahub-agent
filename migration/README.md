# Migration

> **Type:** Archive | **Status:** Frozen | **Scope:** Pre-migration adapter baseline

This directory holds the frozen pre-migration parity harness. It is not active
code — it pins the adapter adoption baseline at 35/35 checks against the public
`workspace-json/agents-audit` repository. See
[`docs/provenance.md`](../docs/provenance.md) for the adoption ruling and
provenance trail.
